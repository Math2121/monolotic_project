export default interface AddClientInputDto {
    name: string;
    email: string;
    address: string;
}

export default interface AddClientOutputDto {
    id: string;
    name: string;
    email: string;
    address: string;
}